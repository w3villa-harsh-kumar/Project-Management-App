
<nav class="navbar navbar-expand-lg bg-dark navbar-dark py-3 mb-4">
  <div class="container">

    <%= link_to ("<i class='my-icon fas fa-project-diagram'></i> Project Management App").html_safe, root_path, class: 'navbar-brand' %>

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <% if current_user && current_user.is_admin? %>
          <li class="nav-item">
            <%= link_to "<i class='fa fa-users'></i> Add Members".html_safe, new_member_path, class: 'navbar-link btn btn-primary me-2' %>
          </li>
          <li class="nav-item">
            <%= link_to "<i class='fa fa-briefcase'></i> Change Plan".html_safe, edit_plan_path, class: 'btn btn-primary navbar-link' %>
          </li>
        <% end %>
      </ul>
      <div class="d-flex">
      <ul class="navbar-nav">
        <% if current_user %>
          <li class="col-md-9 nav-item">
            <%= link_to ('<i class="fa fa-user"></i> ' + truncate(current_user.email, length: 25)).html_safe, edit_user_registration_path, title: 'Edit Profile', class: 'nav-link' %>
          </li>
          <li class="col-md-3 logout">
            <%= button_to 'Logout', destroy_user_session_path, class:'btn btn-danger', method: :delete, data: { turbo: false } %>
          </li>
        <% else %>
          <li>
            <%= link_to('Login', new_user_session_path, class: 'btn btn-primary') %>
          </li>
        <% end %>
        </ul>
      </div>
    </div>
  </div>
</nav>